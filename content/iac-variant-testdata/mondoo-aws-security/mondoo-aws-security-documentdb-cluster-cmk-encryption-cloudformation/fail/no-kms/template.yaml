AWSTemplateFormatVersion: "2010-09-09"
Resources:
  MyCluster:
    Type: AWS::DocDB::DBCluster
    Properties:
      MasterUsername: admin
      MasterUserPassword: "{{resolve:secretsmanager:docdb:SecretString:password}}"
      StorageEncrypted: true
