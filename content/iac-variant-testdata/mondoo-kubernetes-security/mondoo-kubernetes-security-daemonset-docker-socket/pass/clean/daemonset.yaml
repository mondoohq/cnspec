apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemonset-docker-socket
  namespace: default
spec:
  selector:
    matchLabels:
      app: daemonset-docker-socket
  template:
    metadata:
      labels:
        app: daemonset-docker-socket
    spec:
      containers:
      - name: app
        image: nginx:1.25
