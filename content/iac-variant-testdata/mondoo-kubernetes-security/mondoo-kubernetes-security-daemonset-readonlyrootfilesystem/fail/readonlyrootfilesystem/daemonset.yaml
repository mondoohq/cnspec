apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemonset-readonlyrootfilesystem
  namespace: default
spec:
  selector:
    matchLabels:
      app: daemonset-readonlyrootfilesystem
  template:
    metadata:
      labels:
        app: daemonset-readonlyrootfilesystem
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          readOnlyRootFilesystem: false
