apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-hostipc
  namespace: default
spec:
  selector:
    matchLabels:
      app: deployment-hostipc
  template:
    metadata:
      labels:
        app: deployment-hostipc
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
