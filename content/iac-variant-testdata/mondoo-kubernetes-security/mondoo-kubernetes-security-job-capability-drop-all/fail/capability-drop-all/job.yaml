apiVersion: batch/v1
kind: Job
metadata:
  name: job-capability-drop-all
  namespace: default
spec:
  template:
    metadata:
      labels:
        app: job-capability-drop-all
    spec:
      containers:
      - name: app
        image: nginx:1.25
      restartPolicy: Never
