apiVersion: batch/v1
kind: Job
metadata:
  name: job-hostipc
  namespace: default
spec:
  template:
    metadata:
      labels:
        app: job-hostipc
    spec:
      containers:
      - name: app
        image: nginx:1.25
      hostIPC: true
      restartPolicy: Never
