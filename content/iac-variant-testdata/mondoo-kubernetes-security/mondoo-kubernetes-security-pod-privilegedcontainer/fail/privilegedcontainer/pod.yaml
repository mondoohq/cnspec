apiVersion: v1
kind: Pod
metadata:
  name: pod-privilegedcontainer
  namespace: default
  labels:
    app: pod-privilegedcontainer
spec:
  containers:
  - name: app
    image: nginx:1.25
    securityContext:
      privileged: true
