apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-capability-drop-all
  namespace: default
spec:
  selector:
    matchLabels:
      app: replicaset-capability-drop-all
  template:
    metadata:
      labels:
        app: replicaset-capability-drop-all
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
