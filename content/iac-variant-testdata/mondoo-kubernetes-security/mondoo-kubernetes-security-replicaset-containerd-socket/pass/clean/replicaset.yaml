apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-containerd-socket
  namespace: default
spec:
  selector:
    matchLabels:
      app: replicaset-containerd-socket
  template:
    metadata:
      labels:
        app: replicaset-containerd-socket
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
