apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-hostpid
  namespace: default
spec:
  selector:
    matchLabels:
      app: replicaset-hostpid
  template:
    metadata:
      labels:
        app: replicaset-hostpid
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
