apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset-hostpid
  namespace: default
spec:
  selector:
    matchLabels:
      app: statefulset-hostpid
  template:
    metadata:
      labels:
        app: statefulset-hostpid
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
  serviceName: statefulset-hostpid
