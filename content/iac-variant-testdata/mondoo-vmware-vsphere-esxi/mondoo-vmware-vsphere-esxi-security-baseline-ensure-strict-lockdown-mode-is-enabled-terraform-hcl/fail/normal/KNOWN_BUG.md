# Known bug: missing-check

Check `mondoo-vmware-vsphere-esxi-security-baseline-ensure-strict-lockdown-mode-is-enabled-terraform-hcl` is not present in this bundle yet; it is added by a separate pull request. This fixture is kept so it will validate automatically once the check lands. Until then the scan reports no applicable policy.

Remove this marker when the underlying fix lands and this scenario asserts correctly.
