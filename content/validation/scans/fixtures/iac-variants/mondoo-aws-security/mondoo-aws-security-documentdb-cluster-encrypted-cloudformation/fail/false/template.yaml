AWSTemplateFormatVersion: "2010-09-09"
Resources:
  DocDBCluster:
    Type: AWS::DocDB::DBCluster
    Properties:
      DBClusterIdentifier: my-docdb-cluster
      MasterUsername: admin
      MasterUserPassword: "{{resolve:secretsmanager:docdb/password}}"
      StorageEncrypted: false
