apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-allowprivilegeescalation
  namespace: default
spec:
  schedule: '*/5 * * * *'
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: cronjob-allowprivilegeescalation
        spec:
          containers:
          - name: app
            image: nginx:1.25
            securityContext:
              allowPrivilegeEscalation: true
          restartPolicy: Never
