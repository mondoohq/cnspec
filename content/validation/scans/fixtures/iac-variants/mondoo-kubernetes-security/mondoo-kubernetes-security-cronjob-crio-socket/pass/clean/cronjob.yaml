apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-crio-socket
  namespace: default
spec:
  schedule: '*/5 * * * *'
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: cronjob-crio-socket
        spec:
          containers:
          - name: app
            image: nginx:1.25
          restartPolicy: Never
