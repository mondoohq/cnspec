apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-privilegedcontainer
  namespace: default
spec:
  schedule: '*/5 * * * *'
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: cronjob-privilegedcontainer
        spec:
          containers:
          - name: app
            image: nginx:1.25
            securityContext:
              privileged: true
          restartPolicy: Never
