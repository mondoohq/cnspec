apiVersion: batch/v1
kind: CronJob
metadata:
  name: cronjob-readonlyrootfilesystem
  namespace: default
spec:
  schedule: '*/5 * * * *'
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: cronjob-readonlyrootfilesystem
        spec:
          containers:
          - name: app
            image: nginx:1.25
            securityContext:
              readOnlyRootFilesystem: false
          restartPolicy: Never
