apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemonset-hostpath-readonly
  namespace: default
spec:
  selector:
    matchLabels:
      app: daemonset-hostpath-readonly
  template:
    metadata:
      labels:
        app: daemonset-hostpath-readonly
    spec:
      containers:
      - name: app
        image: nginx:1.25
        volumeMounts:
        - name: hp
          mountPath: /data
          readOnly: false
      volumes:
      - name: hp
        hostPath:
          path: /data
