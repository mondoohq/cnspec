apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: daemonset-runasnonroot
  namespace: default
spec:
  selector:
    matchLabels:
      app: daemonset-runasnonroot
  template:
    metadata:
      labels:
        app: daemonset-runasnonroot
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          runAsNonRoot: false
