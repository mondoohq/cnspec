apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-hostpid
  namespace: default
spec:
  selector:
    matchLabels:
      app: deployment-hostpid
  template:
    metadata:
      labels:
        app: deployment-hostpid
    spec:
      containers:
      - name: app
        image: nginx:1.25
  replicas: 1
