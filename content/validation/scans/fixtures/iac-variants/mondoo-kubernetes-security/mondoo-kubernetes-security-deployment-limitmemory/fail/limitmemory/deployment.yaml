apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-limitmemory
  namespace: default
spec:
  selector:
    matchLabels:
      app: deployment-limitmemory
  template:
    metadata:
      labels:
        app: deployment-limitmemory
    spec:
      containers:
      - name: app
        image: nginx:1.25
        resources:
          limits:
            cpu: 500m
  replicas: 1
