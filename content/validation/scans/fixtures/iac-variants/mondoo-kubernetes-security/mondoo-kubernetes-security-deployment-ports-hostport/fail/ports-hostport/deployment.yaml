apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-ports-hostport
  namespace: default
spec:
  selector:
    matchLabels:
      app: deployment-ports-hostport
  template:
    metadata:
      labels:
        app: deployment-ports-hostport
    spec:
      containers:
      - name: app
        image: nginx:1.25
        ports:
        - containerPort: 8080
          hostPort: 8080
  replicas: 1
