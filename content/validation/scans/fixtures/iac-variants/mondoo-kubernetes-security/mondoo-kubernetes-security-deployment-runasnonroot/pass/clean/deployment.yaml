apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-runasnonroot
  namespace: default
spec:
  selector:
    matchLabels:
      app: deployment-runasnonroot
  template:
    metadata:
      labels:
        app: deployment-runasnonroot
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          runAsNonRoot: true
  replicas: 1
