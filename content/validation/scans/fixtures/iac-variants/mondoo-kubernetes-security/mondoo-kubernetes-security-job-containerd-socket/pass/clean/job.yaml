apiVersion: batch/v1
kind: Job
metadata:
  name: job-containerd-socket
  namespace: default
spec:
  template:
    metadata:
      labels:
        app: job-containerd-socket
    spec:
      containers:
      - name: app
        image: nginx:1.25
      restartPolicy: Never
