apiVersion: batch/v1
kind: Job
metadata:
  name: job-hostpid
  namespace: default
spec:
  template:
    metadata:
      labels:
        app: job-hostpid
    spec:
      containers:
      - name: app
        image: nginx:1.25
      hostPID: true
      restartPolicy: Never
