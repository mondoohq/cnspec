apiVersion: v1
kind: Pod
metadata:
  name: pod-ports-hostport
  namespace: default
  labels:
    app: pod-ports-hostport
spec:
  containers:
  - name: app
    image: nginx:1.25
    ports:
    - containerPort: 8080
      hostPort: 8080
