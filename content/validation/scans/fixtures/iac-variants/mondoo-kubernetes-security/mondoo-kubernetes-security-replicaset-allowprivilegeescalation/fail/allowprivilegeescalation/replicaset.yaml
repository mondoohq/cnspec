apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-allowprivilegeescalation
  namespace: default
spec:
  selector:
    matchLabels:
      app: replicaset-allowprivilegeescalation
  template:
    metadata:
      labels:
        app: replicaset-allowprivilegeescalation
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          allowPrivilegeEscalation: true
  replicas: 1
