apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: replicaset-readonlyrootfilesystem
  namespace: default
spec:
  selector:
    matchLabels:
      app: replicaset-readonlyrootfilesystem
  template:
    metadata:
      labels:
        app: replicaset-readonlyrootfilesystem
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          readOnlyRootFilesystem: false
  replicas: 1
