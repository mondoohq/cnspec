apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset-docker-socket
  namespace: default
spec:
  selector:
    matchLabels:
      app: statefulset-docker-socket
  template:
    metadata:
      labels:
        app: statefulset-docker-socket
    spec:
      containers:
      - name: app
        image: nginx:1.25
        volumeMounts:
        - name: sock
          mountPath: /var/run/docker.sock
      volumes:
      - name: sock
        hostPath:
          path: /var/run/docker.sock
  replicas: 1
  serviceName: statefulset-docker-socket
