apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset-hostpath-readonly
  namespace: default
spec:
  selector:
    matchLabels:
      app: statefulset-hostpath-readonly
  template:
    metadata:
      labels:
        app: statefulset-hostpath-readonly
    spec:
      containers:
      - name: app
        image: nginx:1.25
        volumeMounts:
        - name: hp
          mountPath: /data
          readOnly: false
      volumes:
      - name: hp
        hostPath:
          path: /data
  replicas: 1
  serviceName: statefulset-hostpath-readonly
