apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefulset-runasnonroot
  namespace: default
spec:
  selector:
    matchLabels:
      app: statefulset-runasnonroot
  template:
    metadata:
      labels:
        app: statefulset-runasnonroot
    spec:
      containers:
      - name: app
        image: nginx:1.25
        securityContext:
          runAsNonRoot: false
  replicas: 1
  serviceName: statefulset-runasnonroot
