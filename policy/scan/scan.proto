syntax = "proto3";

package cnspec.policy.scan;
option go_package = "go.mondoo.com/cnspec/policy/scan";

import "cnquery/motor/inventory/v1/inventory.proto";
import "policy/policy.proto";

message Job {
  mondoo.motor.inventory.v1.Inventory inventory = 1;
  cnspec.policy.PolicyBundle bundle = 2;  
  
  bool do_record = 20;
  repeated string policy_filters = 21;

  // Vault configuration + credentials query
	// Report type configuration
}

