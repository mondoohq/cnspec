# Copyright (c) Mondoo, Inc.
# SPDX-License-Identifier: BUSL-1.1
version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "scan_db.sql"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        emit_json_tags: true
        emit_interface: true
        emit_prepared_queries: true
