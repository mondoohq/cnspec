# MQL for Microsoft 365 Samples

## Retrieve the Root Domain

<Example>
 <MQL>

```mql
microsoft.domains.where(isRoot == true && authenticationType == "Managed").map(id).first
```

 </MQL>
 <Description>
Retrieves the managed root domain identifier for the Microsoft 365 tenant.
 </Description>

</Example>
